export class Customer {
    constructor(dateOfBirth,
                gender,
                address) {
        this.dateOfBirth = dateOfBirth;
        this.gender = gender;
        this.address = address;
    }
}
